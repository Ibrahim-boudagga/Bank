import 'package:flutter/material.dart';

class WalletAnimationScreen extends StatelessWidget {
  const WalletAnimationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
